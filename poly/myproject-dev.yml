server:
  port: 12100

management:
  endpoints:
    web:
      exposure:
        include: "health,info,refresh" # 필요한 엔드포인트 추가
  endpoint:
    health:
      enabled: true
      show-details: always
    beans:
      enabled: true
    info:
      enabled: true
    refresh:    # 추가
      enabled: true
  server:
    address: localhost
    port: 12101

poly: 'BigData Dev! Change!!!!!, POST MAN Change!!!!'
