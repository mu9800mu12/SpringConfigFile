server:
  port: 12100

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      enabled: true
      show-details: always
    beans:
      enabled: true
    info:
      enabled: true
    refresh :
      enabled: true
  server:
    address: localhost
    port: 12101

poly: 'BigData Dev! Change!!!!!, POST MAN Change!!!!'